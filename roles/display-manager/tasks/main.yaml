---
- name: 'Install Xorg'
  ansible.builtin.apt:
    name: '{{ item }}'
    state: present
    install_recommends: false
  loop:
    - 'xorg'
    - 'xinit'

- name: 'Install desktop-base'
  ansible.builtin.apt:
    name: 'desktop-base'
    state: present
    install_recommends: false
